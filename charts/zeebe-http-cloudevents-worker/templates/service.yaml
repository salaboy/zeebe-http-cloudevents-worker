apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: cloudevents-go
spec:
  template:
    spec:
      containers:
      - image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        env:
        - name: K_SINK
          value: "cloudevents-java.default.example.com"
